curl --create-dirs -o $HOME/.postgresql/root.crt 'https://cockroachlabs.cloud/clusters/1364f656-5e2c-4397-9810-dbe1e2ecd8ab/cert'
# schema in case you were wondering:
CREATE TABLE IF NOT EXISTS notes (
    note_id INT8 NOT NULL DEFAULT unique_rowid(),
    title VARCHAR(60) NULL,
    content VARCHAR NULL,
    date TIMESTAMP NULL,
    CONSTRAINT "primary" PRIMARY KEY (note_id ASC),
    FAMILY "primary" (note_id, title, content, date)
);
